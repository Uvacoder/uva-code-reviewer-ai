import { extendTheme, ThemeConfig } from "@chakra-ui/react";

export const customTheme: ThemeConfig = extendTheme({
  colors: {
    blue: {
      200: "#DCEAF9",
      500: "#455097",
    },
  },
});
